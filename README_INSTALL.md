# README_INSTALL.md (DEPRECATED)

Этот файл устарел. Основные инструкции по установке находятся в `README.md`.

См. `README.md` для шагов установки окружения, установки PyTorch+CUDA 12.1 и установки зависимостей через `pip install -r requirements.txt`.
